---
"@homebridge-plugins/homebridge-roomba": patch
---

Fix duplicate `devices[].idleWatchInterval` in config.schema.json layout and add missing `email`/`password` fields to `RoombaPlatformConfig` in settings.ts
