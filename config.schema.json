{
    "layout": [
        "devices[].stopBehaviour",
        "devices[].accessoryCategory",
        "devices[].idleWatchInterval",
        "devices[].cleanBehaviour",
        // Other properties
    ]
}